import React from "react";

const Gallery = () => {
  return (
    <section
      id="gallery"
      className="min-h-screen bg-gradient-to-r from-orange-500 flex items-center justify-center"
    >
      <div className="max-w-4xl mx-auto text-center ">
        <h2 className="text-4xl sm:text-5xl font-bold mb-4">Galeria</h2>
      </div>
    </section>
  );
};

export default Gallery;
